// ============================================
// موتور محاسبه فرمول‌های دینامیک حقوق و دستمزد
// ============================================
//
// این موتور عبارات محاسباتی را ارزیابی می‌کند.
// عبارات شامل متغیرهای داخل {} هستند که از منابع مختلف مقداردهی می‌شوند.
//
// منابع متغیرها (sourceType):
//   - setting: فیلد از PayrollSetting (مثل insuranceRate, minDailyWage)
//   - item: مبلغ یک آیتم حقوقی دیگر (مثل HOUSING, FOOD)
//   - employee_field: فیلد از اطلاعات کارمند (مثل childrenCount, maritalStatus)
//   - constant: مقدار ثابت عددی
//   - computed: مقادیر محاسبه‌شده داخلی (مثل totalAllowances, totalDeductions, insurableAmount)
//   - tax: محاسبه مالیات تصاعدی (نیازمند پله‌های مالیاتی)
//
// توابع ویژه:
//   - MIN(a, b): حداقل دو عدد
//   - MAX(a, b): حداکثر دو عدد
//   - ROUND(x): گرد کردن
//   - IF(condition, trueVal, falseVal): شرط
//   - TAX_PROGRESSIVE(taxableIncome, year): محاسبه مالیات تصاعدی
//   - CEIL(x): سقف

import { db } from '@/core/lib/db'




// ─── ارزیاب عبارت امن ───

/**
 * عبارات محاسباتی را به صورت امن ارزیابی می‌کند.
 * فقط عملیات ریاضی و توابع مجاز را پشتیبانی می‌کند.
 * از eval استفاده نمی‌شود.
 */




